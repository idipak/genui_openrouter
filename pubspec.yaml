name: genui_openrouter
description: A GenUI transport client for OpenRouter, supporting streaming, fallbacks, and advanced LLM parameters.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  genui: ^0.9.0
  http: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
